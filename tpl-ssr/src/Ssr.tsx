import React from 'react';

import { StaticRouter, HashRouter} from 'react-router-dom';

import App from './App';

const Router = () => (
    <StaticRouter>
      <App/>
    </StaticRouter>
)
export default Router;
