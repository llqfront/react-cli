import React from 'react';

import { BrowserRouter, HashRouter} from 'react-router-dom';

import App from './App';

const Router = () => (
    <HashRouter>
        <App/>
    </HashRouter>
)
export default Router;
